name: Storage block test
on: workflow_dispatch
jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello $(date)" > test.txt
      - uses: actions/upload-artifact@v4
        with:
          name: storage-block-test
          path: test.txt
          retention-days: 1
